"""Template inicial cardápio whitelabel — referência para o architect."""

CARDAPIO_SPEC_HINT = """
Template sugerido para cardápio digital whitelabel:
- frontend: HTML/CSS/JS ou React + painel admin
- backend: FastAPI + SQLite/PostgreSQL
- auth: login admin por tenant
- whitelabel: logo, cores, nome do restaurante por slug/subdomínio
- CRUD: categorias, itens, preços, fotos
"""
